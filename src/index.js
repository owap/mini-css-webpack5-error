import React from 'react';
import ReactDOM from 'react-dom';
import './Dashboard.scss';

const element = <h1>Checking scss compatibility in webpack 5</h1>;

ReactDOM.render(element, document.getElementById('root'));
